// Example keymap (replace with your layout)
#define LAYER_DEFAULT 0

[keymap]
layers = [
    // Default layer (add your keycodes here)
    [ 
        &kp A, &kp B, &kp C, 
        &bt BT_CLR, &bt BT_SEL 1  // Bluetooth reset/pairing keys
    ]
];
